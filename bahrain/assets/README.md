# Brand assets

Drop files here, then point at them from `_content/bahrain.json`.

| File to add | Set in bahrain.json | Used on |
|---|---|---|
| `flooss-logo.svg` (or .png) | `"FLOOS_LOGO": "/assets/flooss-logo.svg"` | The Gold and Platinum rows of the founding value stack. It sits on a light card, so the full-colour navy + orange version is the right one. |
| `tara-and-daisy.jpg` | (wire on request) | Founding page, Tara's section |
| `daisy-portrait.jpg` | (wire on request) | Founding page + Daisy's story |

Until a file exists, the build strips the empty `<img>` so nothing ships broken.
